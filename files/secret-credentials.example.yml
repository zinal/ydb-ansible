# Sensitive logging credentials.
# Copy to files/secret-credentials.yml, customize and encrypt with Ansible Vault.
# Variable names must match those used by roles directly.

ydb_fluentbit_credentials: "fluentbit:password"
loglugger_server_ydb_auth_login: "loguser"
loglugger_server_ydb_auth_password: "P@$$w0rd+"
